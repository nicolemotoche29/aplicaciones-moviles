import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-firebas-file-upload',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
